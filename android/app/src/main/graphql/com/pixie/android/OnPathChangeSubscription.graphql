subscription onPathChange($gameSessionId:Float!){
    onPathChange(gameSessionId:$gameSessionId){
        id
        order
        points

        primaryColor
        opacityPrimaryColor
        strokeWidth

    }
}