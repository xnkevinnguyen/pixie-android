mutation CreateGame($input: CreateGameInput!){
    createGame(input:$input){
        id
        currentDrawerId
        currentWord
        currentRound
        status
        sprintTries
        gameHall{
            id,
            name,
            participants{
                id
                username
                isOnline,
                avatar
            }
        }
        gameInfo{
            id,
            players{
                id,
                username,
                isOnline,
                avatar
            }
            mode,
            language
        }
    }
}